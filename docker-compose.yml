version: "3"
services:
  api:
    build: api
    ports:
      - 3000:3000
    depends_on:
      - db
    env_file:
      - development.env
    networks:
      - recipe-network
  db:
    build: db
    env_file:
      - development.env
    networks:
      - recipe-network

networks:
  recipe-network:
    external: true